#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cout << "Enter length of integer array bloomDay: ";
    cin >> n;

    vector<int> nums(n);
    cout << "Enter " << n << " elements: ";
    for(int i = 0; i < n; i++) {
        cin >> nums[i];
    }

    int m;
    cout << "Enter number of bouquets to make i.e. m: ";
    cin >> m;

    int n;
    cout << "Enter number of adjacent flowers that can be taken i.e. n: ";
    cin >> n;



    cout << endl;
    return 0;
}

// UNFINISHED RN